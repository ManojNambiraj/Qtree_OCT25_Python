# Operators:

    # Arithmetic operators
        # (+, -, *, /, %, //, **)

            # a = 5
            # b = 3

            # result = a + b
            # result = a - b
            # result = a * b
            # result = a / b
            # result = a % b
            # result = a // b
            # result = a ** b     # 5 * 5 * 5

            # print(result)

    # Assignment operators
        # (=, +=, -=, *=, /=, %=, //=, **=)

            # a = 5

            # a += 3      # a = a + 3

            # print(a)

    # Comparison operators   --> Boolean (True, False))
        # (==, !=, <, >, <=, >=)

            # age = 18

            # result = (age == 18)
            # result = (age != 18)
            # result = (age < 18)
            # result = (age >= 18)

            # print(result)

    # Logical operators
    # Identity operators
    # Membership operators
    # Bitwise operators
